'use strict';const fs=require('node:fs'),path=require('node:path');const root=path.resolve(__dirname,'../..');for(const file of fs.readdirSync(__dirname)){if(!file.endsWith('.cjs'))continue;const s=fs.readFileSync(path.join(__dirname,file),'utf8');if(/require\([^)]*(src\/|Reducer|SchelmRuntime)/.test(s))throw new Error(`oracle imports production: ${file}`);}console.log('oracle import gate pass');
