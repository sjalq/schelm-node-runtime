'use strict';const fs=require('node:fs'),crypto=require('node:crypto');const o=require('./oracle/oracle.cjs');const file=process.argv[2];if(!file)throw new Error('usage: replay <json>');const input=JSON.parse(fs.readFileSync(file,'utf8'));let events;if(input.subsystem==='console')events=o.consoleOracle(input.trace);else if(input.subsystem==='signal')events=o.signalOracle(input.trace);else if(input.subsystem==='ticker')events=o.tickerOracle(input.target,input.interval,input.actual);else if(input.subsystem==='input')events=o.inputOracle(input.chunks,input.eof);else throw new Error('bad subsystem');const canonical=JSON.stringify(events);const digestInput={...input};delete digestInput.digest;const digest=crypto.createHash('sha256').update('schelm-node-runtime-trace-v1\n'+JSON.stringify(digestInput)+'\n'+canonical+'\n').digest('hex');console.log(JSON.stringify({subsystem:input.subsystem,digest,events}));if(input.digest&&input.digest!==digest)process.exit(2);
