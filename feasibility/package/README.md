# Runtime feasibility fixture

Not a production package. This fixture probes the pinned Schelm compiler's private
kernel package, effect-manager, Task, command callback, subscription cleanup, and
standalone process-exit behavior. Its API is intentionally disposable.
