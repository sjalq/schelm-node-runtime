# Final API compile fixture
Disposable compile-only package. It proves the frozen public signatures and effect-manager topology compile in debug and optimized applications. It provides no Node runtime behavior and is not production code.
